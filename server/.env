# Copy this to server/.env and fill in your creds

# --- Sogni authentication ---
SOGNI_USERNAME=hillarydickorydock
SOGNI_PASSWORD=r9gzwdb2W
SOGNI_APP_ID=sogni-onepiece-ideas
# one of: local | staging | production
SOGNI_ENV=production

# --- server settings ---
PORT=3001

# Comma-separated list of allowed frontends (CORS origins)
# Keep localhost for local dev. Add your deployed web origin(s) as needed.
CLIENT_ORIGIN=http://localhost:5173