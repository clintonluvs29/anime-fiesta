# Copy to web/.env (local) or web/.env.production (build-time)
# If your API is behind the same domain or dev proxy, leave this empty.
# For a deployed API on another origin, set it like:
# VITE_API_BASE_URL=https://your-api.example.com
VITE_API_BASE_URL=http://localhost:5173